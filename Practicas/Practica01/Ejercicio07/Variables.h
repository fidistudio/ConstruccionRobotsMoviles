#ifndef VARIABLES_H
#define VARIABLES_H

// Pines
const int PIN_PWM = 3;          // Salida PWM que define referencia
const int PIN_COMPARAR = 2;     // Entrada digital del comparador

// Variables
int pwmReferencia = 0;          // PWM calibrado que se usará como referencia

#endif
